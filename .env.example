# example environment configuration for DAO Fetcher
# copy to ".env" in the project root 

# Required for onchain (Tally) requests
TALLY_API_KEY=your_tally_api_key

# optional: improves Snapshot request limits
SNAPSHOT_API_KEY=your_snapshot_api_key

# optional: client-side rate limit for Tally requests (1-60). Omit to use default.
# TALLY_REQS_PER_MIN=30
